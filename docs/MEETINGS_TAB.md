# Вкладка "Встречи" на странице проекта

## Описание

На странице проекта добавлена новая вкладка "Встречи" с функционалом создания и редактирования встреч.

## Функционал

### 📋 Основные возможности:

1. **Просмотр встреч**
   - Список всех встреч проекта
   - Информация: название, дата/время, статус, оценка
   - Визуальные индикаторы статуса (Запланирована/Завершена)

2. **Поиск встреч**
   - Поле поиска по названию встречи
   - Фильтрация в реальном времени

3. **Создание встречи**
   - Кнопка "+ Создать встречу"
   - Открывает модальное окно с формой

4. **Редактирование встречи**
   - Клик на карточку встречи открывает модаль редактирования
   - Обновление всех параметров встречи

## Модальное окно встречи

### Поля формы:

- **Название** - текстовое поле с названием встречи
- **Заметки** - большое текстовое поле для заметок (textarea)
- **Планируемая дата/время** - datetime input
- **Оценка** - числовое поле (0-10)
- **Статус** - выпадающий список (Не завершена / Завершена)
- **Список задач** - чек-боксы для выбора задач встречи

### Кнопки:
- **Отмена** - закрыть форму без сохранения
- **Сохранить** - сохранить встречу и закрыть форму

## Структура компонентов

```
ProjectDetailPage (page.tsx)
├── MeetingModal (компонент для формы встреч)
├── Вкладка "Встречи"
│   ├── Поле поиска
│   ├── Кнопка создания
│   └── Список встреч
│       └── Meeting Card (кликабельная карточка)
```

## Состояние и логика

### State переменные:

```typescript
// Вкладка на странице
const [tab, setTab] = useState<'desc' | 'meetings'>('desc');

// Управление модалью
const [showMeetingModal, setShowMeetingModal] = useState(false);
const [selectedMeeting, setSelectedMeeting] = useState<Meeting | undefined>();

// Данные встреч
const [meetings, setMeetings] = useState<Meeting[]>([...]);

// Поиск
const [query, setQuery] = useState('');
```

### Обработчики событий:

- `handleCreateMeeting()` - открыть модаль для создания новой встречи
- `handleEditMeeting(meeting)` - открыть модаль для редактирования встречи
- `handleSaveMeeting(meetingData)` - сохранить встречу (создать или обновить)

### Фильтрация:

```typescript
const filteredMeetings = useMemo(() => 
  meetings.filter(m => 
    m.description?.toLowerCase().includes(query.toLowerCase()) ?? false
  ),
  [meetings, query]
);
```

## Интеграция с Redux (в будущем)

Для полной интеграции с Redux нужно:

1. Создать `meetingsSlice.ts` в `store/slices/`
2. Добавить actions для загрузки, создания, обновления встреч
3. Подключить в главный store
4. Заменить локальный state на Redux selectors и dispatch

## Mock данные

При загрузке проекта инициализируется массив встреч:

```typescript
const [meetings, setMeetings] = useState<Meeting[]>([
  {
    id: 'meeting-1',
    projectId: projectId,
    description: 'Планирование спринта',
    dateTime: new Date(Date.now() + 86400000).toISOString(),
    resultMark: 5,
    isFinished: false,
  },
  // ...
]);
```

## Стиль интерфейса

- Использует компоненты дизайн-системы проекта
- Tailwind CSS для стилизации
- Консистентен с остальным интерфейсом приложения
- Адаптивный дизайн

## API Endpoints (для будущей интеграции)

```
GET /api/projects/{projectId}/meetings
- Получить все встречи проекта

POST /api/projects/{projectId}/meetings
- Создать новую встречу

PUT /api/projects/{projectId}/meetings/{meetingId}
- Обновить встречу

DELETE /api/projects/{projectId}/meetings/{meetingId}
- Удалить встречу
```
