# 🚀 Быстрый старт

## Система лайков и дизлайков кейсов

### Использование:

```tsx
import CaseLikeButton from '@/components/CaseLikeButton';

// В компоненте
<CaseLikeButton 
  caseId="case-1"
  userId={currentUser?.id}
  showCounts={true}
/>
```

### Где найти:

- Страница: `/requests` - вкладка "Кейсы"
- Каждая карточка кейса имеет кнопки лайка/дизлайка в нижней части

### Как это работает:

1. **Пользователь видит** 👍 и 👎 кнопки
2. **Клик на кнопку** → отправляется голос на backend
3. **Счетчик обновляется** → показывает всё количество лайков/дизлайков
4. **Цвет кнопки меняется** → показывает голос пользователя

---

## Вкладка "Встречи" на странице проекта

### Использование:

1. Откройте любой проект (`/projects/[id]`)
2. Найдите вкладку "Встречи" (рядом с "Описание")
3. Нажмите "+ Создать встречу"

### Функции:

**Создание встречи:**
- Кнопка "+ Создать встречу"
- Заполните форму
- Нажмите "Сохранить"

**Редактирование встречи:**
- Кликните на карточку встречи
- Отредактируйте поля
- Нажмите "Сохранить"

**Поиск:**
- Используйте поле поиска сверху
- Фильтруется по названию встречи

### Поля встречи:

- **Название** - название встречи
- **Заметки** - подробное описание
- **Дата и время** - когда проводится встреча
- **Оценка** - 0-10 баллов
- **Статус** - Запланирована / Завершена
- **Список задач** - выбор задач для встречи

---

## Примеры кода

### Redux использование (лайки):

```typescript
import { useDispatch, useSelector } from 'react-redux';
import { submitVote } from '@/store/slices/votesSlice';
import { selectCaseVoteStats } from '@/store/selectors';

// Получить статистику лайков
const stats = useSelector(selectCaseVoteStats('case-1'));
console.log(stats); // { likes: 5, dislikes: 2, total: 7 }

// Отправить голос
dispatch(submitVote({
  caseId: 'case-1',
  userId: 'user-123',
  reactionType: 'like'
}));
```

### Управление встречами:

```typescript
// Создание встречи
handleCreateMeeting();

// Редактирование встречи
handleEditMeeting(meeting);

// Сохранение встречи
handleSaveMeeting({
  description: 'Новая встреча',
  dateTime: '2024-11-15T10:00:00',
  resultMark: 5,
  isFinished: false
});

// Поиск встреч
setQuery('планирование');
```

---

## 📝 Интеграция с API

### Текущее состояние:

- ✅ Frontend структура готова
- ✅ Mock данные для разработки
- ✅ Функции для API вызовов
- ⏳ Backend endpoints нужны

### Когда backend готов:

Обновите endpoints в:

1. **Лайки** - `src/services/api-votes.ts`
```typescript
// Замените имитацию на реальные API вызовы
const response = await fetch(`/api/cases/${caseId}/votes`, {
  method: "POST",
  // ...
});
```

2. **Встречи** - обновите `src/app/projects/[id]/page.tsx`
```typescript
// Загрузите встречи с API вместо mock
const meetings = await fetchProjectMeetings(projectId);
```

---

## 🎨 Кастомизация

### Изменить цвета лайков:

В `CaseLikeButton.tsx` строка 53-65:
```tsx
isLiked
  ? 'bg-green-100 text-green-700'  // ← измените цвета
  : 'bg-gray-100 text-gray-600'
```

### Изменить иконки:

Строка 55 и 67:
```tsx
<span className="text-lg">👍</span>  // ← поменяйте эмодзи
<span className="text-lg">👎</span>  // ← или используйте иконки
```

### Изменить поля встречи:

В `MeetingModal.tsx` - добавьте новые `<input>` поля в форму

---

## ❓ FAQ

**Q: Как удалить встречу?**
A: Функция удаления пока не реализована. Можно добавить кнопку "Удалить" в MeetingModal.

**Q: Мой голос за кейс не сохраняется?**
A: Это нормально - используются mock данные. Backend нужен для постоянного хранения.

**Q: Можно ли добавить еще вкладок?**
A: Да! Добавьте еще один case в `setTab` и еще одну кнопку-таб.

**Q: Как подключить реальный API?**
A: Смотрите раздел "Интеграция с API" выше.

---

## 🔗 Ссылки на документацию

- [VOTING_SYSTEM.md](./VOTING_SYSTEM.md) - подробно о системе лайков
- [MEETINGS_TAB.md](./MEETINGS_TAB.md) - подробно о вкладке встреч
- [IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md) - общее резюме
